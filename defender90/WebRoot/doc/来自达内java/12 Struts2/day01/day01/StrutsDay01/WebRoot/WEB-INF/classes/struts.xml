<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE struts PUBLIC
    "-//Apache Software Foundation//DTD Struts Configuration 2.1.7//EN"
    "http://struts.apache.org/dtds/struts-2.1.7.dtd">

<struts>

	<!-- 
		struts.xml主要体现请求与Action的关系，
		以及Action与JSP的关系。
		/StrutsDay01/demo/hello.action
		/项目部署名/NAMESPACE/ACTION_NAME.action
		.action后缀是固定的，也可以省略。
	 -->
	 <!-- 
	 	请求执行过程：
	 	1.请求发给filter
	 	2.filter根据请求URL读取struts.xml
	 	3.filter找到namespace="/demo"的package
	 		元素，其中demo是URL项目名的下级；
	 	4.filter在该package元素下，找到name="hello"
	 		的action元素，其中hello是/demo的下级；
	 	5.filter根据找到的action元素，可知要调用的
	 		类名和方法名，于是它会实例化该类并调用
	 		其方法；
	 	6.方法执行结束，filter根据其返回的字符串，
	 		在当前的action元素下，找到name="ok"的
	 		result元素，并将请求转发到该result元素
	 		下声明的页面上；
	  -->
	 <!-- 
	 	package元素是对action元素的打包。
	 	name是package的包名，用于引用这个package；
	 	namespace是package包的部署名，用于访问
	 		这个package，必须以/开头；
	 	extends指当前的package继承于哪一个package，
	 		它的值是另一个package元素的名称。继承
	 		于哪个package，就相当于把这个package下
	 		所有的内容复制到了当前的package下。
	  -->
	 <package name="hello-demo"
	 	namespace="/demo"
	 	extends="struts-default">
	 	<!-- 
	 		action用来注册Action类及方法；
	 		name是注册的元素名，类似于Spring容器
	 			中bean的id功能，用于请求访问Action
	 			类及方法的；
	 		class是要注册的Action的类名；
	 		method是要注册的Action的方法名；
	 	 -->
	 	<action name="hello"
	 		class="com.tarena.action.HelloAction"
	 		method="sayHello">
	 		<!-- 
	 			result是用来实现页面转发的；
	 			name是result元素名，用来匹配Action
	 				方法返回的字符串；
	 			result元素中的内容，是用来声明请求
	 				转发到的页面；
	 		 -->
	 		<result name="ok">
	 			/WEB-INF/jsp/hello.jsp
	 		</result>
	 	</action>
	 </package>

</struts>